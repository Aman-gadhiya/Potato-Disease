# Web app framework
streamlit>=1.35.0

# Deep learning model runtime (loads the .keras model and runs predictions)
tensorflow-cpu>=2.16.0,<2.22.0

# Image loading / resizing / format conversion
Pillow>=10.0.0

# Tabular data handling for the evaluation-metrics pages
pandas>=2.0.0

# Required by pandas' Styler.background_gradient() used on the confusion matrix
matplotlib>=3.7.0

# Numerical array operations for image preprocessing
numpy>=1.24.0
